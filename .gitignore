# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# MLflow local scratch (the real stores live in Postgres + the artifact store)
mlruns/
mlflow.db

# Secrets: the official stack's .env is generated from .env.dev.example, never committed
mlflow-stack/.env
.env

# MLflow local dev server scratch (Phase 1, no Docker) - see docs/INSTALLED.md
mlartifacts/

# Project instructions/docs: kept locally, not tracked in git history
docs/
CLAUDE.md
AGENTS.md

# drift_monitor.py's local Continuous Training trigger-gate state (Phase 8)
analysis/.drift_history.json
